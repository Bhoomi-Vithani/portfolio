import React__default from 'react';

const ChartXYPlotPresentation = ({ width, height, children }) => (React__default.createElement("svg", { width: width >= 0 ? width : 0, height: height >= 0 ? height : 0 }, children));
ChartXYPlotPresentation.displayName = "ChartXYPlotPresentation";

export { ChartXYPlotPresentation };
