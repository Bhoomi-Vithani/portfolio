import React__default from 'react';
import { ActionButtonGroupPresentation } from './shared/ActionButtonGroupPresentation.js';

const ActionButtonGroup = (props) => React__default.createElement(ActionButtonGroupPresentation, { ...props });
ActionButtonGroup.displayName = "ActionButton.Group";

export { ActionButtonGroup };
